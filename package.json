{
  "private": true,
  "name": "contract-fixer-guard",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check": "node --version && node tests/artifact-constants.test.js && node tests/artifact-shape.test.js"
  }
}
